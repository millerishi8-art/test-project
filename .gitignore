# Dependencies - do not commit (reproducible from package.json + package-lock.json)
node_modules/

# Environment and local data (never commit credentials)
.env
.env.local
server/.env
*.log
# Local JSON dumps under server/data (optional). Tracked: codes.js (ISO country list for casesController on Linux/Vercel).
server/data/*.json

# Build output
dist/
build/
.vite/

# OS and editor
.DS_Store
Thumbs.db
*.swp
*.swo
