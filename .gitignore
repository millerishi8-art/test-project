# Dependencies - do not commit (reproducible from package.json + package-lock.json)
node_modules/

# Environment and local data (never commit credentials)
.env
.env.local
server/.env
*.log
# Only ignore server data folder (cases.json etc.). Keep client/src/data/ for benefitsFallback etc.
server/data/

# Build output
dist/
build/
.vite/

# OS and editor
.DS_Store
Thumbs.db
*.swp
*.swo
